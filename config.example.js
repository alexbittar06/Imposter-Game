/**
 * Firebase config for Imposter Game.
 * 1. Copy this file to config.js:  cp config.example.js config.js
 * 2. Replace the placeholders with your project values from Firebase Console.
 * config.js is gitignored so your API key is never committed.
 */
var FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "G_XXXXXXXXXX"
};
